DATABASE_URL=postgresql://...           # local or shared dev DB
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=<generate via openssl rand -base64 32>
GOOGLE_CLIENT_ID=<from your localhost OAuth client>
GOOGLE_CLIENT_SECRET=<from your localhost OAuth client>
OPENAI_API_KEY=sk-...
BLOB_READ_WRITE_TOKEN=...
NEXT_PUBLIC_DEV_USER_EMAIL=your.dev@email.com  # dev-only bypass (never set in production)
